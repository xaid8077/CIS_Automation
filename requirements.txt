# requirements.txt
# Pin major versions; let patch versions float for security fixes.

# ── Core framework ────────────────────────────────────────────────────────────
flask>=3.0.0,<4.0.0
werkzeug>=3.0.0,<4.0.0

# ── Database ──────────────────────────────────────────────────────────────────
flask-sqlalchemy>=3.1.0,<4.0.0
psycopg2-binary>=2.9.9,<3.0.0

# ── Migrations ────────────────────────────────────────────────────────────────
flask-migrate>=4.0.5,<5.0.0

# ── Auth — session (web UI) ───────────────────────────────────────────────────
flask-login>=0.6.3,<1.0.0

# ── Auth — JWT (REST API) ─────────────────────────────────────────────────────
flask-jwt-extended>=4.6.0,<5.0.0

# ── Password hashing ──────────────────────────────────────────────────────────
argon2-cffi>=23.1.0,<24.0.0

# ── Forms & CSRF ──────────────────────────────────────────────────────────────
flask-wtf>=1.2.1,<2.0.0
wtforms>=3.1.0,<4.0.0
email-validator>=2.1.0,<3.0.0

# ── Rate limiting ─────────────────────────────────────────────────────────────
flask-limiter>=3.5.0,<4.0.0

# ── Validation / serialisation ────────────────────────────────────────────────
marshmallow>=3.20.0,<4.0.0

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2.0.0

# NOTE: openpyxl is intentionally omitted.
# Excel generation uses direct XML + zipfile manipulation (stdlib only)
# via utils/excel_writer.py + utils/embedded_templates.py.
# No third-party Excel library is required.